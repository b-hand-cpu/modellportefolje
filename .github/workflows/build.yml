name: Bygg modellportefølje

on:
  schedule:
    # 18:00 UTC på hverdager – godt etter stengetid på Oslo Børs (16:20/16:30 lokal tid,
    # både i CET og CEST) slik at dagens sluttkurser er tilgjengelige hos yfinance.
    - cron: '0 18 * * 1-5'
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - 'stocks/**'
      - 'templates/**'
      - 'scripts/**'
      - 'requirements.txt'
      - 'data/holdings.csv'
  pull_request:
    paths:
      - 'stocks/**'
      - 'templates/**'
      - 'scripts/**'
      - 'requirements.txt'
      - 'data/holdings.csv'

permissions:
  contents: write

concurrency:
  group: build-index
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Sjekk ut repo
        uses: actions/checkout@v4

      - name: Sett opp Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Installer avhengigheter
        run: pip install -r requirements.txt

      - name: Kjør oppdater.py
        run: python scripts/oppdater.py

      - name: Kjør build_index.py
        run: python scripts/build_index.py

      # Ved pull_request stopper vi her – bygget skal bare valideres, ikke committes.
      - name: Commit og push oppdaterte data
        if: github.event_name != 'pull_request'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html data/performance.csv data/per_aksje.csv
          if git diff --cached --quiet; then
            echo "Ingen endringer – hopper over commit."
          else
            git commit -m "Oppdater portefølje [skip ci]"
            git push
          fi
